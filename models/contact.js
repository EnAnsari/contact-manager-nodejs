class Contact {
    constructor(id, fullname, imageUrl, mobile) {
        this.id = id;
        this.fullname = fullname;
        this.imageUrl = imageUrl;
        this.mobile = mobile;
    }
}

module.exports = {
    Contact
};